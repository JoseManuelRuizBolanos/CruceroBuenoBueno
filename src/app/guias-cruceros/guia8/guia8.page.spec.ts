import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Guia8Page } from './guia8.page';

describe('Guia8Page', () => {
  let component: Guia8Page;
  let fixture: ComponentFixture<Guia8Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Guia8Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
