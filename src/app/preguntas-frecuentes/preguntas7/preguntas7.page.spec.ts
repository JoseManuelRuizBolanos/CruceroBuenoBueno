import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Preguntas7Page } from './preguntas7.page';

describe('Preguntas7Page', () => {
  let component: Preguntas7Page;
  let fixture: ComponentFixture<Preguntas7Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Preguntas7Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
