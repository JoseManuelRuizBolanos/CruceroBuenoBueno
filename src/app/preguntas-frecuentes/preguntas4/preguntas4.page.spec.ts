import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Preguntas4Page } from './preguntas4.page';

describe('Preguntas4Page', () => {
  let component: Preguntas4Page;
  let fixture: ComponentFixture<Preguntas4Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Preguntas4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
