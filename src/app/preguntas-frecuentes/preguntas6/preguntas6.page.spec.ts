import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Preguntas6Page } from './preguntas6.page';

describe('Preguntas6Page', () => {
  let component: Preguntas6Page;
  let fixture: ComponentFixture<Preguntas6Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Preguntas6Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
