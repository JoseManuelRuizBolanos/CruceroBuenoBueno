import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Preguntas9Page } from './preguntas9.page';

describe('Preguntas9Page', () => {
  let component: Preguntas9Page;
  let fixture: ComponentFixture<Preguntas9Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Preguntas9Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
