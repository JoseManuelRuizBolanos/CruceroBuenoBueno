import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Preguntas12Page } from './preguntas12.page';

describe('Preguntas12Page', () => {
  let component: Preguntas12Page;
  let fixture: ComponentFixture<Preguntas12Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Preguntas12Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
