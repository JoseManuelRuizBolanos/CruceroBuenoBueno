import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Preguntas5Page } from './preguntas5.page';

describe('Preguntas5Page', () => {
  let component: Preguntas5Page;
  let fixture: ComponentFixture<Preguntas5Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Preguntas5Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
