import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Preguntas8Page } from './preguntas8.page';

describe('Preguntas8Page', () => {
  let component: Preguntas8Page;
  let fixture: ComponentFixture<Preguntas8Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Preguntas8Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
