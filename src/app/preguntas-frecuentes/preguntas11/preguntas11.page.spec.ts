import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Preguntas11Page } from './preguntas11.page';

describe('Preguntas11Page', () => {
  let component: Preguntas11Page;
  let fixture: ComponentFixture<Preguntas11Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Preguntas11Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
