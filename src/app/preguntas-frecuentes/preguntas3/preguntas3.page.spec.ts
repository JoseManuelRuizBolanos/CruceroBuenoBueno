import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Preguntas3Page } from './preguntas3.page';

describe('Preguntas3Page', () => {
  let component: Preguntas3Page;
  let fixture: ComponentFixture<Preguntas3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Preguntas3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
