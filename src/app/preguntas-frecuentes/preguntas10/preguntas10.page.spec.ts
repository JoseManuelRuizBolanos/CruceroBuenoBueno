import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Preguntas10Page } from './preguntas10.page';

describe('Preguntas10Page', () => {
  let component: Preguntas10Page;
  let fixture: ComponentFixture<Preguntas10Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Preguntas10Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
